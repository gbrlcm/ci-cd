# ============================================================
# .env EXAMPLE — AMBIENTE PRODUÇÃO
# Copie para .env e edite antes de subir o ambiente.
# Nunca comite o .env real (valores sensíveis).
# ============================================================

APP_PORT=8080

MONGO_DATABASE=cidades_esg
MONGO_USERNAME=root
MONGO_PASSWORD=troque-esta-senha-producao
MONGO_AUTH_DB=admin

JWT_SECRET=troque-este-secret-producao-muito-longo-com-mais-de-32-caracteres

# Imagem GHCR (opcional quando usada via CD/CI)
# IMAGE=ghcr.io/seuusuario/cidades-esg:latest