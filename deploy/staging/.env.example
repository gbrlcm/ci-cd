# ============================================================
# .env EXAMPLE — AMBIENTE STAGING
# Copie para .env e edite antes de subir o ambiente.
# Nunca comite o .env real (valores sensíveis).
# ============================================================

APP_PORT=8081

MONGO_DATABASE=cidades_esg_staging
MONGO_USERNAME=root
MONGO_PASSWORD=troque-esta-senha-staging
MONGO_AUTH_DB=admin

JWT_SECRET=troque-este-secret-staging-muito-longo-com-mais-de-32-caracteres

# Imagem GHCR (opcional quando usada via CD/CI)
# IMAGE=ghcr.io/seuusuario/cidades-esg:latest